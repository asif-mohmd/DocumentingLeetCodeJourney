            let target = nums[i + 1];
            let count = map[target] + 1 || 1;
            map[target] = count;
            if (count > maxCount) {
                maxVal = target;
            }
        }
    }
        if (nums[i] == key) {
    return maxVal;
};
    let maxCount = 0;
    for (let i = 0; i < nums.length - 1; i++) {    
                maxCount = count;
var mostFrequent = function(nums, key) {
    let map = {}
    let maxVal;
 * @return {number}
 */
/**
 * @param {number[]} nums
 * @param {number} key
[
