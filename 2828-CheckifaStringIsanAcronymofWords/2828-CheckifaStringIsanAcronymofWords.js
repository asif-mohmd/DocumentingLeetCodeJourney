/**
 * @param {string[]} words
 * @param {string} s
 * @return {boolean}
 */
var isAcronym = function(words, s) {

    for(let i=0;i<words.length;i++){
         str += words[i].charAt(0)
    }
    return s == str
    let str = ''
};
[
